build:
  context: ./backend
  dockerfile: Dockerfile

deploy:
  command: python manage.py migrate --noinput

start:
  command: gunicorn wifi_occupancy_prediction_project.wsgi:application --bind 0.0.0.0:$PORT
